en:
  errors:
    not_found:
      title: Not Found
      detail: Nothing exists at that URI
    bad_request:
      title: Bad Request
      detail: Bad bad bad request
    unauthorized:
      title: Unauthorized
      detail: Wrong email or password
  activerecord:
    errors:
      models:
        boards:
          attributes:
            board_properties:
              invalid_json: "Invalid Board properties, strips is required one."
